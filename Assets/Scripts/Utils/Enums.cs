public enum CardType
{
    Instant,
    Attack,
    Powerup
}

public enum PlayerAttribute
{
    Power,
    Spirit,
    Weight,
    Reflex,
    Critical
}

public enum PlayerType
{
    Player, 
    Opponent
}